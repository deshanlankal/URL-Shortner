{
  "name": "url_shortner_api",
  "version": "1.0.0",
  "description": "A simple and efficient API for shortening URLs, allowing users to generate short links that redirect to original URLs. It supports basic functionalities like generating, retrieving, and managing shortened URLs. Ideal for integration into web applications to track and simplify long URLs.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Deshan Lanka",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.1",
    "nanoid": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "typescript": "^5.8.2"
  }
}
